name: "order-service"

replicaCount: 1

image:
  repository: muhirwajd/order-service
  tag: "v1"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5002
  targetPort: 5002

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
